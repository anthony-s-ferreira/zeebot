# Zee Assistant — dependências Python (Raspberry Pi OS 64 bits / Python 3.9+)

Flask>=3.0,<4.0            # servidor HTTP local + SSE
Werkzeug>=3.0,<4.0         # servidor WSGI com threads (vem com o Flask)
vosk>=0.3.45; platform_system != "Darwin"  # reconhecimento offline no Raspberry Pi/Linux
vosk>=0.3.44,<0.3.45; platform_system == "Darwin"  # última wheel disponível no macOS
sounddevice>=0.4.6         # captura de áudio via PortAudio (requer libportaudio2)
rapidfuzz>=3.6             # busca tolerante (fuzzy matching)
qrcode>=7.4                # QR Code do Wi-Fi em SVG (não requer Pillow)
llama-cpp-python>=0.2.90; platform_system == "Linux" or platform_system == "Darwin"  # SLM GGUF local no Raspberry Pi e macOS
piper-tts>=1.3,<2; platform_system == "Linux"  # leitura offline das respostas em pt-BR
